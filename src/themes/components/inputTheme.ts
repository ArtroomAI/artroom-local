const inputTheme = {
    // The styles all button have in common
    defaultProps: {
        borderWidth: '1px',
        borderRadius: '1px',
        borderStyle: 'solid',
        borderColor: '#FFFFFF20'
    }
};

export default inputTheme;
